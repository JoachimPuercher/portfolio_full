# Public base URL, baked into the static export at build time
# (canonical URLs, hreflang, sitemap.xml, Open Graph, JSON-LD).
# Use the address the files will be served from, e.g. a test subdomain first.
NEXT_PUBLIC_SITE_URL=https://www.puercherjoachim.com
